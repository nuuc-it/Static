# Static

Static host for NUUC's Google Apps Script front ends, served by GitHub Pages from `main`/root at
<https://nuuc-it.github.io/Static/>.

Built output only — never hand-edited. See **[PUBLISHERS.md](PUBLISHERS.md)** for which project repo
owns which folder under `pub/`, and for the ownership map the publishing pipeline validates against.
